/* tslint:disable */
/* eslint-disable */

import type { CSSProperties, FunctionComponent } from 'react';
#rpx-1:import Taro from '@tarojs/taro';:#
import Icon from './rn';

export type IconNames = '#names#';

interface Props {
  name: IconNames;
  size?: number;
  color?: string | string[];
  style?: CSSProperties;
}

const IconFont: FunctionComponent<Props> = ({ name, size = #size#, color, style, ...rest }) => {
  return <Icon name={name} size={#rpx-1:parseFloat(Taro.pxTransform(size)):##rpx-0:size:#} color={color} style={style} {...rest} />;
};

export default IconFont;
